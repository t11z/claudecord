# ── Claude authentication ────────────────────────────────────────────────
# claudecord has NO shared, instance-wide Claude credential. Every Discord
# user links their own Claude Code OAuth token via `/link-claude link` (a
# Discord modal — never a channel message), generated on any machine with
# Claude Code installed:
#   claude setup-token
# There is nothing to configure here.

# ── Discord ──────────────────────────────────────────────────────────────
# Bot token from https://discord.com/developers/applications
# Remember to enable the "Message Content Intent" on the Bot page!
DISCORD_BOT_TOKEN=

# Application (client) ID — used to register slash commands and build the
# invite URL shown in the dashboard.
DISCORD_APPLICATION_ID=

# ── Per-user GitHub (optional) ───────────────────────────────────────────
# claudecord has no shared, instance-wide GitHub token either. To let members
# use their own GitHub account in agentic runs, register a GitHub App
# (Settings → Developer settings → GitHub Apps) with "Enable Device Flow"
# checked, then set its credentials here. Members run /link-github to connect
# their OWN account via the OAuth Device Flow; agentic runs act in the acting
# user's namespace. Leave unset to skip GitHub integration entirely.
# GITHUB_APP_CLIENT_ID=
# GITHUB_APP_CLIENT_SECRET=

# ── Dashboard ────────────────────────────────────────────────────────────
# There is no dashboard password. Sign-in is a one-time link sent by the
# `/dashboard` Discord command — see the "Dashboard accounts" doc page.
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=3000

# Base URL the bot uses to build that sign-in link. Defaults to
# http://localhost:<DASHBOARD_PORT>, which is only correct when the browser
# you sign in from is on the same machine as the bot. Set this explicitly to
# whatever address/domain actually reaches the dashboard otherwise.
# DASHBOARD_PUBLIC_URL=https://claudecord.example.com

# Comma-separated Discord user IDs that are always dashboard admins. Without
# this, the first person to run /dashboard while holding Manage Guild on a
# server becomes the first admin automatically — set this instead if you
# want that decided explicitly rather than by whoever logs in first.
# DASHBOARD_ADMIN_IDS=123456789012345678,234567890123456789

# ── Bot behaviour ────────────────────────────────────────────────────────
# Default model for new conversations. Admins can override per guild.
CLAUDE_MODEL=claude-sonnet-5

# Where SQLite, per-thread workspaces and other state live.
DATA_DIR=./data

# Maximum Claude runs across all guilds at once (each run is a subprocess).
MAX_CONCURRENT_RUNS=4

# Log level: trace | debug | info | warn | error
LOG_LEVEL=info
